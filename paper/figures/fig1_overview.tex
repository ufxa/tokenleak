% Figure 1: TokenLeak Framework Overview -- B&W single-column
\begin{tikzpicture}[
    box/.style={draw=black, rounded corners=3pt, align=center, font=\small,
                minimum height=0.75cm, text width=2.0cm},
    wide/.style={draw=black, rounded corners=3pt, align=center, font=\small,
                 minimum height=0.75cm, text width=\columnwidth-2\pgflinewidth-4pt},
    llmbox/.style={wide, fill=white, line width=1.2pt},
    subbox/.style={box, fill=gray!15, line width=0.6pt},
    probe/.style={box, fill=gray!10, line width=0.8pt},
    agent/.style={box, fill=gray!30, line width=1.0pt},
    trcsbox/.style={wide, fill=gray!8, line width=1.2pt},
    outbox/.style={wide, fill=gray!40, line width=1.2pt},
    arrow/.style={-Stealth, thick, black},
    darrow/.style={-Stealth, thick, dashed, black},
    lbl/.style={font=\footnotesize\itshape, black}
]

\def\W{6.4}   % total width
\def\col{2.0} % column cell width
\def\gap{0.2} % gap between cells
\def\cx{1.1}  % col 1 center x
\def\cy{3.3}  % col 2 center x
\def\cz{5.5}  % col 3 center x

% ---- Row 0: LLM Engine (full width) ----
\node[llmbox, text width=\W cm] (llm) at (\W/2, 5.2)
    {\textbf{Target LLM Inference Engine}\\[1pt]
     {\footnotesize GPT-2 / BERT / LLaMA-2}};

% sub-labels inside LLM box
\node[subbox, text width=1.7cm] (gpu) at (0.9, 5.2)
    {\textbf{GPU/CPU}\\[-1pt]{\footnotesize Shared HW}};
\node[subbox, text width=1.7cm] (kvc) at (5.5, 5.2)
    {\textbf{KV Cache}\\[-1pt]{\footnotesize L1/L2/LLC}};

% ---- side-channel label ----
\node[lbl] at (\W/2, 4.55) {\textit{side-channel leakage}};

% ---- Row 1: Probes ----
\node[probe, text width=1.9cm] (timing) at (\cx, 4.0)
    {\textbf{Timing Oracle}\\[-1pt]{\footnotesize inter-token $\Delta t$}};
\node[probe, text width=1.9cm] (cache) at (\cy, 4.0)
    {\textbf{Cache Profiler}\\[-1pt]{\footnotesize Flush+Reload}};
\node[probe, text width=1.9cm] (net) at (\cz, 4.0)
    {\textbf{Network Probe}\\[-1pt]{\footnotesize API latency}};

% ---- Row 2: Agents ----
\node[agent, text width=1.9cm] (toa) at (\cx, 2.7)
    {\textbf{Agent 1 (TOA)}\\[-1pt]{\footnotesize Timing Oracle Agent}};
\node[agent, text width=1.9cm] (cpa) at (\cy, 2.7)
    {\textbf{Agent 2 (CPA)}\\[-1pt]{\footnotesize Cache Profiler Agent}};
\node[agent, text width=1.9cm] (tra) at (\cz, 2.7)
    {\textbf{Agent 3 (TRA)}\\[-1pt]{\footnotesize Token Reconstructor}};

% ---- feature fusion label ----
\node[lbl] at (\W/2, 2.05) {\textit{feature fusion}};

% ---- Row 3: TRCS ----
\node[trcsbox, text width=\W cm] (trcs) at (\W/2, 1.5)
    {\textbf{Token Reconstruction Confidence Score (TRCS)}\\[2pt]
     {\small $\mathrm{TRCS}(\tau,\mathbf{O})=\displaystyle\sum_{i=1}^{N} w_i \cdot \varphi_i(\tau,O_i)\,/\,Z$}};

% ---- Row 4: Output ----
\node[outbox, text width=\W cm] (out) at (\W/2, 0.4)
    {\textbf{Reconstructed Prompt}\\[-1pt]
     {\footnotesize Top-$k$ Ranked Tokens per Position}};

% ---- Arrows: LLM -> Probes (dashed) ----
\draw[darrow] (llm.south -| timing) -- (timing.north);
\draw[darrow] (llm.south)           -- (cache.north);
\draw[darrow] (llm.south -| net)    -- (net.north);

% ---- Arrows: Probes -> Agents (solid) ----
\draw[arrow] (timing.south) -- (toa.north);
\draw[arrow] (cache.south)  -- (cpa.north);
\draw[arrow] (net.south)    -- (tra.north);

% ---- Arrows: Agents -> TRCS (solid) ----
\draw[arrow] (toa.south) -- ++(0,-0.2) -| ([xshift=-1.2cm]trcs.north);
\draw[arrow] (cpa.south) -- (trcs.north);
\draw[arrow] (tra.south) -- ++(0,-0.2) -| ([xshift=1.2cm]trcs.north);

% ---- TRCS -> Output ----
\draw[arrow] (trcs.south) -- (out.north);

% ---- Legend ----
\node[font=\scriptsize, anchor=west] at (0, -0.25) {%
  \raisebox{1pt}{\tikz\draw[darrow, line width=0.7pt](0,0)--(0.7,0);}~Side-channel leakage
  \quad
  \raisebox{1pt}{\tikz\draw[arrow,  line width=0.7pt](0,0)--(0.7,0);}~Internal data flow};

\end{tikzpicture}
